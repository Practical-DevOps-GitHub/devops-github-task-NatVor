name: Assign Code Owner
on:
  push:
    branches:
      - main

jobs:
  assign_code_owner:
    runs-on: ubuntu-latest
    steps:
      - name: Assign Code Owner
        uses: github/codeowners@v2
        with:
          codeowners: |
            * softservedata
        env:
          GITHUB_TOKEN: ${{ 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 }}
